import { NgxTelInputDirective } from './ngx-tel-input.directive';

describe('NgxTelInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxTelInputDirective();
    expect(directive).toBeTruthy();
  });
});
