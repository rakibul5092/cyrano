import { datingPlatforms } from '../modules/user-registration/models/dating-platforms';

describe('DatingPlatforms', () => {
  it('should create an instance', () => {
    expect(datingPlatforms).toBeTruthy();
  });
});
