import { HeightRangeFormatterPipe } from './height-range-formatter.pipe';

describe('HeightRangeFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightRangeFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
